import capitalize from './capitalize.js';

test("Converts the first character of `string` to upper case and the remaining to lower case.", () => {

    
    expect(capitalize('TEE')).toBe('Tee');


   
})